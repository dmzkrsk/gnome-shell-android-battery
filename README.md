Displays Android battery status in Gnome shell topbar

#Notes

Currently works over USB connection

#HOWTO

- Requires `adb`
- Your phone should allow debugging

Check 

```bash
$ adb devices
```

Should show a device id and status `device`.
